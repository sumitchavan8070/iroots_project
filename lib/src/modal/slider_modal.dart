import 'dart:ui';

class SliderModal {
  String? title;
  String? subtitle;
  String? image;
  Color? titleColor;
  Color? subTitleColor;

  SliderModal(
      {this.title,
      this.subtitle,
      this.image,
      this.titleColor,
      this.subTitleColor});
}
